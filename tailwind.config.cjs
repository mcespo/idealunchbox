const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		fontFamily: {
			'sans': ['Helvetica', 'Arial', 'sans-serif'],
		},
		colors: {
			'il-orange': '#ef7216',
		}
	},

	plugins: []
};

module.exports = config;
